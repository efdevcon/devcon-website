import React from 'react'
import { Filter, useFilter } from 'src/components/common/filter'
import { Speaker } from 'src/types/Speaker';

type Props = {
  speakers: Speaker[]
}

const SpeakerFilter = (props: Props) => {
  
  
  return (

  )
}